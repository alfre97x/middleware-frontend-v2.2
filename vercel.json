{
  "$schema": "https://openapi.vercel.sh/vercel.schema.json",
  "buildCommand": "cd ../packages/sdk && npm install && npm run build && cd ../web-alt && npm install && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "cd ../packages/sdk && npm install && cd ../web-alt && npm install",
  "outputDirectory": ".next"
}
